# TiMus

A Chat client for the Ti-84 CE-T that connects via serial to other calculators using the Chat client or to a hardware piece (like a Raspberry Pi), that runs the server.py script.
You need to set a 3 letter name in before hand.
